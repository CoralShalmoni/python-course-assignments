from word_utils import count_words, print_counts

celestial_objects = [
    'Moon', 'Gas', 'Asteroid', 'Dwarf', 'Asteroid', 'Moon', 'Asteroid'
]

# Count and print
word_counts = count_words(celestial_objects)
print_counts(word_counts)
